# aspen-plus-operator 专家团编制方案

> 用女娲（思维框架蒸馏）组建专家团，把「AI 使用 Aspen Plus」从 **操作层技能** 升级为 **世界级工程能力**。
> 编制日期：2026-09-15 ｜ 诊断依据：本机 Aspen V14 实证（203 个官方 bkp + happ.tlb 类型库 + 9764 页离线帮助）

---

## 一、先定义「世界级」，否则无法验收

「超一流水准」是句空话，先钉成 5 条可验收标准：

| # | 标准 | 当前得分 | 判定方式 |
|---|------|---------|---------|
| W1 | **零静默错误** — 单位/量纲错误必须拦在写入前或读出后，不允许静默通过 | ❌ 0 | 本轮核查就查出 5 处单位认知错误，且**在 ENG 模型上恰好成立**（最危险的一类） |
| W2 | **结果自带可信度** — 每个出数附验证路径 + 失效条件声明 | ❌ 0 | 现在跑通即出数，无收敛质量判据、无闭合误差核验、无独立交叉验证 |
| W3 | **长任务零看护** — 1000+ 工况扫描无需人工盯守，失败自动隔离与断点续跑 | ⚠️ 1 | `aspen_com_run.py` 是脚本模板，无容错、无重试、无状态快照 |
| W4 | **自然语言可达** — 工艺工程师不懂 Python 也能完成「建模→扫描→要结论」 | ⚠️ 1 | 需要手写 JSON 工况表 + 懂 COM 路径 |
| W5 | **安全护栏内建** — 越界参数不可执行，输出不可直接下装 DCS | ❌ 0 | 完全没有；用户现场是 2–6 MPa 水解塔，AI 改参数有真实物理风险 |

**结论**：5 条里 3 条为 0、2 条为 1。缺口不在「会不会调 COM」——那已经解决得不错——而在于**可信度、防错、可靠性、易用性、安全**这五件「让工程师敢用」的事。

---

## 二、缺口 → 席位映射

按缺口配思维框架，不按名气配人。每个席位必须能回答：**他的哪个具体镜片，能补这个具体缺口？**

### 席位 1 · 验证官 — Richard Feynman（理查德·费曼，1918–1988）

**补 W2（结果可信度）**

- **核心镜片**：第一性原理 + 「第一个原则是你不能骗自己——而你自己是最容易被骗的人」+ **Cargo Cult Science**（看起来像科学、流程全对、但结果毫无意义）
- **为什么是他**：一个「跑通了但没验证」的 Aspen 模型，就是化工版的 cargo cult science——图标对了、收敛了、数字出来了，但没人问过「你怎么知道它是对的」。费曼式的攻击点全在**独立交叉验证**：不信任软件自己的收敛标志，找一个与模型无关的独立方法去撞它。
- **产出**：`references/verification-protocol.md`
  - 收敛质量判据（不只看 converged 标志 → 物料/能量闭合残差、Design-Spec 残差、塔板效率合理性区间）
  - 三条独立验证路径：① 解析解/手算对照（本项目已有雏形：IAPWS 复核 Psat）② **极限退化检验**（把模型退化到有解析解的情形，看是否收敛到已知值）③ 多物性方法交叉
  - **强制失效条件声明**：每个出数必须附「该结论在 X 条件下失效」
- **局限**：费曼是物理学家，不懂化工单元操作；他给的是验证的**态度与方法**，不是化工判据本身（判据从轨道 B 补）。

### 席位 2 · 人因官 — Don Norman（唐纳德·诺曼，在世）

**补 W1（零静默错误）+ W4（易用性）**

- **核心镜片**：《设计心理学》——**人为错误是设计错误**；affordance / signifier / feedback / 容错设计
- **为什么是他**：本轮查出的单位错误，是典型的「被设计出来的错误」——系统让一个 `<20><2>` 和 `<20><5>` 相差 14.5 倍的错误**静默地发生**，没有任何反馈。Norman 的第一问永远是：「这个错误为什么能被静默地犯下去？系统给了什么 signifier？」这直接命中 W1 的根因。
- **产出**：`references/error-proof-design.md`
  - 写入前断言层：量纲校验 + **量级合理性熔断**（如塔压落在 0.1–100 bar 之外直接拒绝，而不是照写）
  - 读出后交叉验证：同一物理量走两条独立路径读，不一致就告警
  - 反馈可视化规范：扫描结果必须带异常标记，不能只给一张干净的 CSV
  - 易用性：把「JSON 工况表」提升为「自然语言意图 → 参数化」的映射规范
- **局限**：Norman 是认知科学家，不懂 Aspen；且**在世人物**，需标注信息截止日期。

### 席位 3 · 容错官 — Joe Armstrong（乔·阿姆斯特朗，1950–2019）

**补 W3（长任务零看护）**

- **核心镜片**：Erlang/OTP —— **「Let it crash」**、监督树（supervision tree）、故障隔离、进程隔离 + 重启策略
- **为什么是他**：批量扫 203 个 bkp = 203 个可能以各种方式死掉的任务（COM 卡死、引擎不收敛、许可证掉线、bkp 损坏）。Armstrong 的核心洞见是：**不要试图预测所有错误，而是让错误快速失败、隔离、由监督者决定重启/降级/跳过**。这跟「在 Python 里 try-except 一堆 COM 异常」是完全相反的哲学，也正是当前脚本缺的东西。
- **产出**：`references/batch-reliability.md` + `scripts/aspen_swarm.py`
  - 每工况独立 COM 实例/子进程，崩溃不传染主流程
  - 监督者三档策略：Reinit 后重试 → 降级参数重试 → 跳过并记审计
  - 断点续跑：已完成清单落盘，中断后从断点继续
  - 结构化审计日志（每工况：耗时/状态/错误类型/重试次数）
- **局限**：Armstrong 的语境是电信级并发系统（99.9999999% 可用性），化工批量扫描没有那么高的可靠性要求——取其**架构哲学**，不照搬可用性指标。

### 席位 4 · 安全官 — Trevor Kletz（特雷弗·克莱兹，1922–2013）

**补 W5（安全护栏）**

- **核心镜片**：本质安全设计（ISD）——「**你没有的东西，就不会泄漏**」；HAZOP；对计算机控制系统带来**新型事故模式**的专门研究
- **为什么是他**：Kletz 是化工安全领域唯一一个既懂化工、又专门批判过「自动化系统如何制造新事故」的人。AI 自动改工艺参数是一种**全新的危害源**，Kletz 的第一问是：「如果这个输出直接下装，最坏会发生什么？能不能让这个危害**根本不存在**（而不仅仅是概率降低）？」
- **产出**：`references/safety-guardrails.md`
  - 参数变更三级风险分级：只读分析 / 探索性扫描 / 生产指导（第三级强制人工复核）
  - **设计包络熔断**：温度/压力/组分超出设备设计范围，拒绝执行而非警告
  - AI 输出的强制声明规范（哪些结论可下装、哪些仅供讨论）
- **局限**：Kletz 的思想成型于前 AI 时代，没有针对 LLM agent 的现成答案——需要把他方法论**外推**到 AI 场景，外推部分必须标注。

---

## 三、诚实区分：有一条缺口不能靠蒸馏解决

**缺口 W2 的另一半 + 领域深度（物性方法决策、流程模式库）是知识问题，不是思维框架问题。**

蒸馏 Feynman 能让 AI 学会「追问验证」，但 Feynman 不会告诉 AI「2–6 MPa 高压含水体系该选 NRTL 还是 PR-BM」。这个只能从本机的一手资料里提取：

| 轨道 B 任务 | 来源 | 产出 |
|---|---|---|
| 物性方法选择决策树 | 9764 页官方 HtmlHelp | `references/property-method-decision.md` |
| 流程模式库（拓扑 + 典型工况 + 典型参数） | 203 个官方 bkp 结构化提取 | `references/pattern-library.md` |
| 收敛故障排查库 | HtmlHelp + 实测 | 并入 `bkp-diagnostics.md` |

**建议**：轨道 B 与蒸馏并跑，但**不占用女娲流程**（它是知识工程，不是造人）。

---

## 四、推荐编制与档位

### 我的推荐：4 席全上（Feynman / Norman / Armstrong / Kletz）

理由：4 个席位**零重叠**，各自对应一条为 0 或缺分的标准，且四位都有**充足的一手素材**（这是蒸馏质量的前提）：

| 人物 | 最强一手素材 | 可得性 |
|---|---|---|
| Feynman | 1974 Caltech 毕业演讲 *Cargo Cult Science* 全文、《费曼物理学讲义》、Surely You're Joking | 高（公开档案） |
| Norman | NN/g 专栏、*The Design of Everyday Things*、大量长访谈 | 高（在世，需标截止日） |
| Armstrong | 博士论文 *Making reliable distributed systems in the presence of software errors*（全文公开） | 高（公开 PDF） |
| Kletz | IChemE/AIChE 论文、*What Went Wrong?* 系列 | 中高（部分需检索） |

**成本量级（必须先说清楚）**：标准档 4 人 × 6 维度 = **24 个并行调研任务**，是重任务，token 消耗显著。

| 档位 | 调研规模 | 适用 | 相对成本 |
|---|---|---|---|
| 快速 | 每人 3 维度，每维限 5 源 | 先看效果 | ~1/3 |
| **标准（默认）** | 每人 6 维度完整 | 推荐 | 1× |
| 深度 | 6 维度 + 一手素材全量下载 | 打算开源发布 | ~2–3× |

---

## 五、执行路线图

```
Phase 0.5  建目录 ~/.workbuddy/skills/aspen-<role>-perspective/      (4 个)
Phase 1    24 个并行调研 agent，落盘 references/research/01-06.md
Phase 1.5  🔴 调研质量检查点 → 你确认
Phase 2    框架提炼（心智模型 3-7 个 / 启发式 / 表达DNA / 诚实边界）
Phase 2.5  🔴 提炼确认检查点 → 你确认
Phase 3    构建 4 份 SKILL.md（含 Agentic Protocol：遇事实问题先搜索再答）
Phase 4    质量验证（已知/边缘/风格三测）→ 🔴 你确认
Phase 5    双 agent 精炼
─────────────────────────────────────────
交付        4 个 perspective skill + 4 份 references 改进文档 + 改造后的 aspen-plus-operator
```

**蒸馏产物如何回流到项目**：每个 perspective skill 不只是"能对话的角色"，它必须产出一份**落到 aspen-plus-operator 里的具体改进文档**（见各席位"产出"列）。这是本方案与"为了蒸馏而蒸馏"的区别。

---

## 六、待你拍板

见下方选项。选定后立即 Phase 0.5 建目录开工。
