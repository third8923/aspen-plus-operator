# aspen-plus-operator 专家团协同章程

> 四位专家不是四个聊天角色，是一条**流水线上的四道闸门**。
> 编制日期：2026-09-15 ｜ 调研规模：4 人 × 6 维度 = 24 个调研任务，616 KB 一手素材

---

## 一、编制表

| 席位 | 专家 | 负责标准 | 闸门位置 | 心智模型 | 改进文档 |
|---|---|---|---|---|---|
| 验证官 | Richard Feynman（1918–1988） | **W2 结果可信度** | 出数前 | 6 个 | `_workshop/feynman-验证协议.md` |
| 人因官 | Don Norman（1935– ，在世） | **W1 零静默错误** + W4 易用性 | 写参前 | 7 个 | `_workshop/norman-防错设计.md` |
| 容错官 | Joe Armstrong（1950–2019） | **W3 长任务零看护** | 批量执行中 | 7 个 | `_workshop/armstrong-批量容错架构.md` |
| 安全官 | Trevor Kletz（1922–2013） | **W5 安全护栏** | 输出放行前 | 7 个 | `_workshop/kletz-安全护栏.md` |

安装位置：
- **专家包（推荐入口）**：`~/.workbuddy/plugins/marketplaces/my-experts/plugins/aspen-sim-quality-guard/`
  —— 已注册为 WorkBuddy 专家团「**Aspen模拟质保团**」，从左侧「专家」入口直接选中对话。
  主理人**费明证**（验证总监），成员**柯本源**（过程安全工程师）、**诺明因**（人因工程师）、**乔容之**（容错架构师）。
  四份 perspective 技能已作为团队 `skills/` 携带，随专家团一起加载。
- 独立技能（可单独调用）：`~/.workbuddy/skills/<name>-perspective/`
触发方式：专家团直接对话；或说「用费曼视角」「诺曼会怎么看」「Armstrong 视角」「克莱兹怎么看」

---

## 二、四道闸门的串接顺序

```
用户需求
   │
   ▼
┌──────────────────────────────────────────┐
│ 闸门 1 · 诺曼（写参前）                    │
│ 这个错误能不能被静默地犯下去？              │
│ · unit 必填，裸数字 → TypeError            │
│ · 量级 sensibility check（换算因子指纹）    │
│ · 量纲/单位集一致性                        │
└──────────────────────────────────────────┘
   │ 通过
   ▼
┌──────────────────────────────────────────┐
│ 闸门 3 · 阿姆斯特朗（批量执行中）           │
│ 谁负责恢复？故障是瞬态还是确定性？           │
│ · 指纹判定 Bohrbug → 封禁同策略重启         │
│ · 重启预算 5次/30分钟，单工况 max=1         │
│ · 子进程隔离 + 外部看门狗                  │
└──────────────────────────────────────────┘
   │ 每个工况完成
   ▼
┌──────────────────────────────────────────┐
│ 闸门 2 · 费曼（出数前）                    │
│ 飞机落没落下来？你怎么知道它是对的？         │
│ · 复现性检验 C6（Reinit 重跑，>1e-4 判多解）│
│ · 极限退化检验 D1/D3                      │
│ · 强制失效条件声明                         │
└──────────────────────────────────────────┘
   │ 通过
   ▼
┌──────────────────────────────────────────┐
│ 闸门 4 · 克莱兹（输出放行前）               │
│ 这个危害能不能消除？能下装吗？              │
│ · 设计包络熔断 check_envelope             │
│ · 三级风险分级                            │
│ · 措施层级硬排序（本质>被动>主动>程序）      │
└──────────────────────────────────────────┘
   │
   ▼
交付（带失效条件 + 验证路径 + 复核要求）
```

---

## 三、冲突仲裁（真实冲突，不是假想）

四位专家之间有**两处正面对撞**，必须预先裁定，否则改造会自相矛盾。

### 冲突 1：诺曼「减少摩擦」 vs 克莱兹「保留安全摩擦」

- **诺曼**：人为错误是设计错误，摩擦是缺陷，应消除。
- **克莱兹**：最终保护必须独立于控制系统；在安全关键场景，某些摩擦是资产。
- **外部证据**：IEC 62366-2 相关研究指出，**可用性在安全关键系统中是非单调的**——提升某操作可用性常须牺牲另一操作；"为安全需在访问关键设置时设置障碍"。诺曼框架本身没有判断"多少摩擦恰当"的工具。

**裁定**：
> 在**参数写入与结果读取**路径上，听诺曼的——这里要极致顺滑、零静默错误。
> 在**越界与下装**路径上，听克莱兹的——这里必须保留不可跳过的摩擦。

具体落地：
- 写 `TEMP=180` 这类常规参数 → 诺曼：自动补单位、自动校验、顺滑通过
- 写超出设计包络的参数、或输出"生产指导级"结论 → 克莱兹：**拒绝执行**，且不可被提示词绕过（护栏长在代码里，不写在 prompt 里）

### 冲突 2：阿姆斯特朗「快速失败重启」 vs 费曼「不放过任何可疑结果」

- **阿姆斯特朗**：让错误快速失败、由监督者决定。
- **费曼**：一个"通过"的结果可能是假通过，必须复现检验。
- **外部证据**：Jim Gray 指出重启只对**瞬态故障**有效；Aspen 不收敛多为 **Bohrbug（确定性）**，重启只会再失败一次。

**裁定**：
> **先分类，再决定动作。** 分类权归阿姆斯特朗，但分类的判据由费曼提供。

具体落地：
- 故障指纹相同 → 确定性 → **不重启**，跳过并记审计（阿姆斯特朗的预算机制）
- 结果"通过"但无独立验证 → 费曼的 C6 复现性检验**强制执行**，不接受"收敛了就是对的"
- 二者都对"静默成功"零容忍，这是共识，不是冲突

---

## 四、各位专家最硬的一条落地建议

| 专家 | 最硬的一条 | 为什么硬 |
|---|---|---|
| **费曼** | **C6 复现性检验**：出数前显式 Reinitialize 重跑一次，关键输出偏差 >1e-4 即判「存在多解」 | 挡住了唯一一种「所有检查都过、结果仍是假的」情形。批量扫描里报出的"最优工况"可能只是上一次迭代的残影 |
| **诺曼** | **接口层强制约束**：`safe_set(node, value, unit)` 中 unit 必填，裸数字直接 TypeError；新值落在 14.5038 / 1e5 / 273.15 等换算因子 ±1% 内即**拒绝并点名"疑似 psi↔bar"** | 单位错误被判为 **mistake**（意图层错误）而非 slip，意味着提示词工程无效，必须在接口层加约束 |
| **阿姆斯特朗** | **指纹判定封禁重启**：同一工况第二次失败若 `sha256(bkp+入参+阶段+错误码)` 与上次相同即判 Bohrbug，封禁同策略重启；全局预算 5次/30分钟，单工况 max_restarts=1 | 这是切断「1 次失败变 N 次」的唯一机制。Python+COM 六条失效条件全中，照搬 let it crash 会放大故障 |
| **克莱兹** | **护栏长在代码里**：`check_envelope(param, spec)` 焊死在 COM 写参函数**内部**（不是写进 prompt）；规格缺失/单位不可确认/数值为 None 一律 **BLOCK 而非放行** | 免责声明属于"程序层"，是 Kletz 明列的**最不可靠**一层，不得计入护栏 |

---

## 五、调研中发现的三条「打脸」结论

这些推翻了流行说法或项目原有认知，务必记住：

1. **「let it crash」是推论不是原则**。Armstrong 本人 2003 年邮件澄清：真实原则是 "let some other process fix the error"，且**只在进程完全隔离的语言中成立**，他明确排除顺序语言、共享内存线程、Unix 进程。九个九那个数字，他自己在论文 §8.5 承认来源只有一份未说明来源的 PPT。

2. **物性方法可能与官方指导冲突**。官方帮助：活度系数法（含电解质法）压力上限 **10 atm**，ENRTL-HF 仅 3 atm；高压统一口径 >10 bar 应走高级混合规则 EOS（PRWS/PSRK/SR-POLAR）。而本项目用 NRTL 跑 **2–6 MPa**。同时官方明确 SRK 须配 STEAMNBS（方向验证项目判断），但"高估 1.5–2 倍"的定量数字官方查不到。**这条矛盾是费曼式验证的绝佳范例，待实测裁定。**

3. **AI 做安全分析的能力被高估**。2026 年 *Safety Science* 194:107039：GPT-4o/mini/LLAMA/Gemini 生成 HAZOP，F1 >86% 但**语义有效性仅 0.19–0.37**，保障措施严重偏向程序性——恰是 Kletz 列为最不可靠的一层。CCPS RBPS / OSHA PSM / IEC 61511 三份基石标准**均未提 AI**。

> 附一条对本项目直接有利的合规动态：**中国 GB 45673—2025（强制性，2025-11-01 实施）已把「最小化、替代、缓和、简化」写入国标**。这意味着克莱兹这套方法论在国内不是"先进理念"而是**合规要求**。

---

## 六、落地路线（建议顺序）

| 阶段 | 动作 | 依赖 |
|---|---|---|
| P1 | 诺曼：给 `scripts/aspen_units.py` 加断言层与 `safe_set` | 无 |
| P2 | 克莱兹：给 COM 写参函数内嵌 `check_envelope` | 无 |
| P3 | 阿姆斯特朗：重写批量执行器为 `scripts/aspen_swarm.py`（子进程+监督者+预算） | P1、P2 |
| P4 | 费曼：把验证清单与 C6 复现性检验写进 `SKILL.md` 的"出数前必过"章节 | P3 |
| P5 | 轨道 B：把 `property-method-decision.md`、`pattern-library.md`、`convergence-playbook.md` 并入 `references/` | 无，可并行 |
| P6 | 物性方法冲突实测裁定（2–6 MPa 该用什么） | P5 |

**注意**：轨道 B 的四份产出目前在 `_workshop/`，尚未并入 `references/`——它们不随技能安装。需要你确认后我再并。

---

## 七、诚实边界

- 四位专家中有三位已去世（费曼 1988、阿姆斯特朗 2019、克莱兹 2013），**没有人见过 LLM**。所有"AI 场景应用"都是外推，改进文档中已逐条标注。
- 诺曼在世（90 岁），信息截止 2026-09-15，建议 12 个月内刷新一次。
- 本团补的是**可信度、防错、可靠性、安全**四件事，**不补化工工艺本身的领域深度**——那靠轨道 B 的官方知识提取，不靠蒸馏。
- 蒸馏质量上限取决于素材：Armstrong 维度的一手素材最扎实（295 页博士论文 + 邮件原文），Kletz 次之，两位在世/近世人物的二手转述占比相对较高。
